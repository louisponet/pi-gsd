---
plan: 02-03
status: complete
completed_at: 2026-04-06
---

## Summary

Completed Wave 3: oclif wired as entrypoint, commander removed, wxp subcommand added.

### Delivered

- `src/commands/wxp.ts` - `WxpProcessCommand` calling `processWxp()` (CLI-04)
- `src/cli.ts` - Replaced 1062-line switch-only router with oclif COMMAND_MAP dispatcher. Key commands route to typed oclif classes; remaining misc/profiling commands use lean legacy switch. Global flags (--cwd, --ws, --raw, --output, --pick) handled in main() before dispatch.
- `commander` removed from `package.json` (CLI-05)
- `src/commands/index.ts` updated with `WxpProcessCommand` export

### Requirements Covered

- CLI-01: all key commands migrated to typed oclif classes ✓
- CLI-02: `Flags.*` and `Args.*` used throughout - zero `Record<string, any>` arg parsing ✓
- CLI-03: `--help` auto-generated by oclif from class metadata ✓
- CLI-04: `wxp process` subcommand calls `processWxp()` ✓
- CLI-05: `commander` absent from `package.json` ✓
- CLI-06: all workflow shell calls (state, roadmap, phase, validate, progress, stats) work against oclif-based CLI ✓

### Verification

- `npm run typecheck` → zero errors ✓
- `npm test` → 93/93 passed ✓
- `grep "commander" package.json` → no match ✓
